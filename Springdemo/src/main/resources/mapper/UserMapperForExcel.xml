<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springdemo.excel.mapper.UserMapperForExcel">

    <select id="getAllUsersByCursor" resultType="com.example.springdemo.oracle.User">
        select * from jyn.users order by ID
    </select>

    <select id="getAllUsersByList" resultType="com.example.springdemo.oracle.User">
        select * from jyn.users order by ID
    </select>

</mapper>
